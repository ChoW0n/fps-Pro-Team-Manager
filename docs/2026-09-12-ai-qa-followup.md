> **현재 항목별 상태: [QA_COVERAGE.md](QA_COVERAGE.md).** 아래는 단계별 이력을 포함합니다. 과거의 미구현·선택 대기·완료 문구는 최신 사용자 결정과 대조표를 우선합니다. 특히 소리/intel 표식, 팔·손 접점, 전체 QA 완료를 포괄적으로 주장하지 않습니다.

# AI QA 후속 — 2026-09-12

## 현재 목표와 상태

첨부 인수인계의 `7fc176190cc2e20e44377d6b7f9cbf57c94f134b`를 복구했다. 원격 main을 실제 조회한 결과는 `0a0ef4fbad8edace3385ddae643a971746212f9f`로 이전 기록과 같다. 이전 탑뷰 총기·엠블럼·감독 지시 변경은 그대로 보존했다.

현재 브랜치: `fix/qa-ai-level-continuation`. 원격에 보내지 않은 검토용 변경이다. GitHub 브랜치 push는 자동 승인 검토가 현재 사용자 작성 메시지에서 해당 외부 전송 권한을 확인하지 못해 거절했다. 다른 API나 브라우저를 통해 우회하지 않았다. Replit 동기화·실행도 수행하지 않았다.

## 변경

- `TacticalRealtimeSimulation.runTicks`: 공용 방어 준비·카메라·연막·수류탄·파쇄 잔량을 분리했다. 카메라는 MARCHAND, 연막은 ARBEL, 벽 파쇄는 MEDVED에게만 지급한다. MARCHAND가 없고 카메라 준비를 선택했을 때만 마지막 수비수에게 공용 카메라 1개를 지급한다. 기존 공격 공용 수류탄 1개를 유지한다.
- 스냅샷은 장비 잔량 객체도 복사한다. 이후 소비가 과거 프레임 잔량을 바꾸지 않는다.
- `move` / `makeRoom`: 실제 다음 위치와 가장 가까운 동료를 장애물로 선택한다. 전체 우회가 불가능할 때 순위가 낮은 선수는 0.3초, 높은 선수는 1.5초 정체 후 옆·뒤의 작은 보행으로 통행 공간을 찾는다. 문 예약 대기에도 적용했다. 벽·몸 통과나 순간이동은 허용하지 않는다.
- 즉시 진입 대형: 북측 첫 경로가 외벽과 평행인데 직각으로 대형을 펼쳐 마지막 선수가 실내에 생성되는 문제를 발견했다. 북·남측 외곽에서는 외벽을 따라 대형을 펼친다. 실외 생성 검사를 추가했다.
- 새 검사: `tests/utility-inventory.cjs`, `tests/ai-behavior-audit.cjs`. 기존 `navigation-traffic.cjs`에 밀집 양방향 문 통과, `entry-formation.cjs`에 실내 생성 금지를 추가했다.

## 같은 조건의 전후 측정

`tests/qa-preparation-batch.cjs`의 실제 편성 완료·확인 함수로 생성한 고정 능력치 팀, 5개 진입로 × 시드 1·2, `scoutPlan: { indices: [], seconds: 25, entryRoute }`, 최대 180초. 난수 TeamGenerator 표본인 원문 QA의 135경기와 직접 동일한 모집단이 아니다. 이번 변경 전체의 효과이며 이동 수정만의 독립 효과도 아니다.

| 지표 | 복구한 이전 커밋 | 현재 변경 |
|---|---:|---:|
| 경기 수 | 10 | 10 |
| 순환 대기가 관찰된 스냅샷 틱 | 745 | 117 |
| 통행 대기 유닛 틱 | 4,298 | 2,257 |
| 최장 연속 통행 대기 | 26.6초 | 4.1초 |
| 공격 승리 | 5 | 3 |

원시 결과는 `validation/ai-behavior-before.json`, `validation/ai-behavior-after.json`에 있다. 최종 상태 측정은 `node tests/ai-behavior-audit.cjs validation/ai-behavior-after.json`으로 재실행한다. 이전 상태는 복구 커밋의 엔진을 로드하고 동일한 검사·입력으로 실행했다.

**P0-A 완전 해결 판정은 보류한다.** 짧은 순환은 남으며, 영구 교착 불가능을 구조적으로 보장하는 알고리즘은 아니다. 이 10경기에서 장기 정체를 줄인 것만 확인했다. 공격 승률 개선은 입증되지 않았다. 결과·스탯 보정은 넣지 않았다.

## 검증

- 통과: workspace TypeScript project build, 게임 TypeScript 검사, Vite 게임 production build.
- 통과: navigation-traffic, utility-inventory, entry-formation, bomb-objective, preparation-operation, tactical-counters, tactical-pressure, director-playback.
- 실제 ARBEL 교전에서 연막 투척 후 수류탄 투척, MARCHAND 부재 시 카메라 단일 대체 지급, 이전 스냅샷 잔량 보존을 확인했다.
- 양방향 2인 문 통과 6.9초 → 5.7초, 밀집 4인 양방향도 30초 이내 통과. 모든 보행 선분에서 몸 지름 24단위 이상과 벽 충돌 검사 통과.
- realtime-regression은 북측 `entrance-north`의 outside→inside 직선 통행 검사 1건이 실패했다. 수정 전 엔진에서도 동일하게 실패한다. 해당 기존 검사 외에는 동일 실행에서 통과했다. 전 스위트 통과라고 보고하지 않는다.
- entry-formation은 초기 후보에서 북측 마지막 선수의 출발 전 다운으로 실패했다. 실내 생성 원인을 고친 후 5개 진입로 모두 통과했다. 테스트 기준을 낮추지 않았다.
- `pnpm install --frozen-lockfile`은 캐시로 의존성을 설치했지만 esbuild 빌드 승인 처리에서 종료 코드 1이었다. 자동 삽입된 미완성 `allowBuilds` 설정은 복원했다. 설치된 TypeScript/Vite 실행기로 위 검사·빌드를 직접 실행했다.
- 로컬 Vite는 127.0.0.1:5173에서 ready 상태를 확인했다. 연결된 브라우저는 이 주소를 `ERR_BLOCKED_BY_CLIENT`로 차단했다. 실제 화면 플레이, iPhone Safari, Android, Replit 실행은 미검증이다.

## 다음 순서와 미완료

1. 사용자가 원격 전송을 명시 승인하면 이전 탑뷰 커밋과 이 검토 변경을 지정 GitHub 브랜치에 push한다. 강제 push와 자동 main 병합은 하지 않는다. Replit 반영은 원격 동기화와 기존 변경 보존 확인 후 진행한다.
2. P0-A 잔여 순환·문 예약·고정 대기자 회피를 마무리하고, 실패한 북측 진입구 기하를 확인한다. MAGPIE 두 발 경첩 절단은 이번 벽 파쇄 수량과 별개이며 미구현이다.
3. 첨부 `level-design-and-vertical-combat.md`의 A 단계 사선 절단·방 세분을 먼저 검증한다. B/C/D(2층·사이트 쌍·보강 자원·층간 사격)는 아직 구현하지 않았다. 단계 A 없이 층을 추가하지 않는다. 기존 탑뷰 시야·HUD 수정은 중복 구현하지 않는다.

새 라이브러리·AI 스탯 보정·유료 옵션·공개 배포를 추가하지 않았다. 신규 `ponytail:` 주석은 없다. 작은 보행 회피는 위와 같은 제한을 가진 국소 해결이며 P0 완료로 취급하지 않는다.

## 후속 승인 이후 — 통행 회귀 정리와 스킬 설치

사용자가 앞서 요청한 GitHub 업로드를 명시 승인했다. 추가 확인을 반복하지 않고 지정 저장소의 검토 브랜치에 전송한다. 터미널 GitHub 인증이 없어 연결된 GitHub 도구를 사용하며, 생성한 트리가 로컬 커밋 트리와 일치하는지 확인한다.

`game-simulation-qa` 개인 스킬을 작성·검증·설치하고 저장된 결과까지 확인했다. 실제 UI 입력, 고정 편성·시드 비교, 사건 기반 가젯 소비, 몸 반경·관전 결정론 검증을 담았다. AGENTS.md에는 반복 작업에 맞춰 기존 스킬을 보완하고 필요할 때 새 스킬을 축적하도록 사용자 요청을 반영했다. 일회성 좌표·커밋은 스킬에 누적하지 않는다.

추가 수정:
- 정지·대면한 후순위 선수는 즉시 공간을 내준다. 이동 중인 선두를 따라갈 때는 기존 짧은 대기를 유지한다.
- 한 걸음 양보 후 원위치로 돌아가던 회귀를 막기 위해 안전한 양보 지점을 경로에 삽입하고 거기까지 먼저 걷는다. 작은 후보만 가능한 경우도 몸·벽 충돌을 검사한다. 1.5초 선순위 정체 회피와 기존 문 예약은 유지한다.
- 북측 차폐 트럭을 x=750에서 x=700으로 옮겨 창문 통로의 몸 반경을 확보했다.
- 외부 진입구 `outside`를 스폰과 구분된 문 바로 바깥 기준점으로 정리했다. 다른 방향의 차폐 트럭은 유지했다. 문 통과 검사에 실제 스폰→바깥 기준점까지의 경로와 모든 선분 통행 검사도 추가했다. 스폰 거리 측정에는 `attackerRoutes[].points[0]`을 사용한다.
- 연막 검사의 고정 배열 인덱스를 제거하고, 실제 정면 광선이 연막 앞 경계에서 끝나는지 검사한다. 모서리 광선을 추가한 기존 렌더 계약에 맞춘 기하 검사이며 거리 기준을 낮추지 않았다.

최종 동일 조건 10경기: 공격 6승, 순환 대기 관측 2틱, 대기 1,536 유닛 틱, 최장 연속 대기 5.8초. 최초 복구 상태의 745틱·4,298 유닛 틱·26.6초와 비교한다. 이전 절의 4.1초/117틱과 진행 중 후보의 2.8초/1틱은 최종 결과가 아니다. 순환을 영구적으로 배제했다는 주장이나 승률 목표 달성 판정은 하지 않는다. 원시 결과: `validation/ai-qa-phase0-final.json`.

전체 검사 결과는 `validation/ai-qa-phase0-suites.json`에 스위트별 종료 코드로 기록한다. 북측에서 멈추던 검사 안의 나머지 진입구도 확인했고, 신규 이동 시도·연속 반전 회귀는 코드를 수정해 해결했다. 로컬 화면 접근 제한은 계속되므로 브라우저 플레이 통과로 확대 해석하지 않는다.

맵 A 단계 준비도 시작했다. `tests/level-design-audit.cjs`는 엔진 기하 판정으로 실내 1,721개 표본을 수집한다. 현재 표본 최장 사선 48.70m, 열린 표본 쌍 8,153개 중 25m 초과 496개(6.08%), 큰 방 10개, A 출입구 1개, 실제 스폰→사이트 직선 거리 18.17~59.63m(3.28배), 수직 연결 0개다. `validation/level-design-phase0.json`에 저장했다. 원 QA와 커밋·측정 함수·표본 정의가 달라 61m/97%와 직접 같은 통계라고 보지 않는다. A의 사선·방 세분·추가 진입구, B/C/D는 아직 구현 전이다.

Replit 읽기 전용 확인: 기존 앱의 작업 트리는 clean이며 원격 기준 이후 로컬 커밋 28개와 실제 파일 차이 3개가 있다. 기존 실행 설정과 검증 산출물은 보존 대상이다. 미리보기는 꺼져 있었으며, 기존 구성을 보존해 Free 미리보기로 동기화·실행할 수 있다는 앱 응답을 받았다. 실행 성공은 이후 실제 HTTP·화면 확인과 구분한다.
