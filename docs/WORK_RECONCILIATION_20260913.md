# 작업·PR 통합 기록 — 2026-09-13

## 정리 범위

GitHub PR #1~#15, 현재 작업과 앞선 두 로컬 체크아웃, 프로젝트 자료 폴더, 다른 대화의 경기 설계 문서 3종을 대조했다. 진행하지 않은 QA·아트·배포를 완료로 바꾸지 않는다.

## PR 처리

| PR | 확인한 상태와 처리 |
|---|---|
| #2~#14 | 이미 main 병합됨. 같은 변경을 재병합하지 않음 |
| #15 | Survivor 원본 기준 캐릭터, 팔 교정, 표시 잔재 제거, 검증 시트와 작업 정리를 통합 |
| #1 | 인증 경계 문서 1개, 현행 main과 충돌. `docs/CODEX_AUTO.md`에 원문의 Cloud 재검증 절을 보존해 #15로 통합 후 중복 PR 종료 |

#1의 기록은 당시 Cloud worker 관찰이다. 이 작업에서 새 로그인이나 실제 모델 전환을 실행했다는 의미가 아니다.

## 로컬 변경 대조

- `3a75d55`와 원격 `430dea9`: 전체 tree `e093d4e1...` 일치. 마지막 52개 파일 업로드 누락 없음.
- 앞선 `db8aeb3`와 원격 `ed403e5`: 전체 tree `82d153ef...` 일치. 5개 로컬 커밋의 원격 대응도 확인.
- 더 오래된 `8a5ef8c`와 원격 조립식 도입 `2a02c65`: 코드·문서 차이 0, 과거 검증 PNG 21개만 다름. 현재 Survivor 결과로 대체된 시안이며 다시 활성화하지 않음.
- 이번 최종 활성 결과는 `validation/survivor-operator-sheet.png`, 원본 비교는 `validation/survivor-reference-comparison.png`.
- 로컬 실험 이미지·기존 브랜치는 파괴적으로 삭제하지 않았다. 버전 차이와 출처는 `architecture/workspace-inventory.json`에 보존했다.

## 문서 정리

- PROJECT_STATE / ART_APPROVED의 과거 본문을 같은 docs 디렉터리의 `*_HISTORY_20260913.md`로 보존해 상대 링크를 유지했다.
- 현재 문서는 확정 기준·검증 한계·다음 작업만 기록한다. 오래된 아트 current 문서에는 이력 표시를 추가했다.
- 경기 명세, 작업 분해, UI 흐름 설계를 `docs/planning/`에 원문 보존하고 우선순위 설명을 추가했다.
- Archify를 실제 실행해 현재 경기 구조 HTML과 명세·검증 영수증을 생성했다. 게임 의존성을 추가하지 않았다.
- 프로젝트 자료의 루트 QA/명세는 10-INBOX-QA, 이미지 원본은 20-ART-REFERENCE, ZIP은 90-ARCHIVE-HANDOFF로 분류한다. 같은 내용의 여러 파일은 삭제하지 않는다.

## 최종 아트 범위

원본 PNG의 머리·몸통·배낭 종횡비를 유지한다. 원본 정지 프레임과 비교해 팔 접합과 권총 파지 거리를 정리하고, 배낭에 덧그리던 팀색/군장색 막대를 제거했다. 원본의 상체 Spine 전체 동작을 이식한 것은 아니다. 무기 20종, 앉기와 방패 자세는 현재 게임에 맞춘 조립을 사용한다.

참고한 조준 토론: https://forum.gdevelop.io/t/top-down-shooter-aiming-issue/38391

몸 중심과 총구가 다르면 조준 오차가 생기며, 근거리 보정은 회전 불안정을 유발할 수 있다는 사례다. 현재 `BroadcastCanvas`는 표시 총구에서 사건의 targetPosition으로 효과를 연결한다. 근거리 총열 축의 각도 오차는 별도 측정 대상이며, 이번에 AI 조준/명중/충돌을 바꾸거나 해결됐다고 주장하지 않는다.

## 다음 개발 방향

**AI 판단·통행 안정화 → 관전 한 라운드 흐름 → 음향·장비**. 상세 완료 조건은 NEXT_WORK.md 하나에서 관리한다. 아트 리셋·여러 파트 동시 착수·모델 인증 재시도를 기본 후속으로 삼지 않는다.

## 이번 실행 검증

- Survivor 렌더러 480개 조건, 신체 PNG 종횡비 검사 통과.
- 이동·조준 2,560개와 팔 접점 100개 통과.
- Node Canvas 중계 16개 조건 렌더링 성공, 최신 이미지와 기록 갱신. 휴대폰 해상도 일부 p95 지연이 커 실기기 성능 통과로 해석하지 않는다.
- TypeScript 및 Vite 프로덕션 빌드 통과.
- Codex 모의 테스트 28/28 통과. 실제 서버 인증/모델 전환은 미검증 유지.
- Archify deliver 9/9, 오류·경고 0. 프로젝트 지침에 따라 브라우저 visual-check는 미실행.
