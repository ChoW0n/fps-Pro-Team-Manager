# DRAFT ORDER — 에이전트 작업 시작 규칙

이 저장소에서 작업하는 모든 AI 에이전트는 코드·기획·UI·문서·테스트 등 관련 작업을 시작하기 전에 아래 순서를 따른다.

1. `docs/초원_AI_게임개발_협업_마스터프롬프트.md`를 먼저 읽고 현재 작업에 적용한다.
2. 작업을 **FAST / NORMAL / DEEP** 중 하나로 분류한다. 가능한 한 FAST에서 시작하고, 실제 복잡성이 확인될 때만 NORMAL → DEEP으로 승격한다.
3. 작업 시작 시 사용자에게 반드시 **에이전트/환경 + 실제 확인된 모델 + 요청/권장 모델 + 작업 단계**를 한 줄로 선언한다.
4. 실제 모델을 확인할 수 없는 환경에서는 절대로 추정 모델을 `현재 모델` 또는 `사용 모델`이라고 쓰지 않는다.
5. 단계나 모델이 바뀌었다고 주장하려면 전환 전후의 런타임·제품 UI·상태 정보 등 실제 근거가 있어야 한다.
6. 명확한 문제를 과도하게 분석하지 않는다. 실행해서 빠르게 확인 가능한 것은 작은 수정 → 실행/검증을 우선한다.
7. 최신 사용자 요청과 실제 저장소 상태가 문서보다 우선한다.
8. 작업 범위를 불필요하게 확장하지 않는다.
9. 구현 후에는 수행한 검증과 미확인 사항을 구분한다.

## 사용자 플랜 및 비용 정책

- 사용자는 **Pro*5 플랜에서 사용 가능한 모든 모델**을 작업 특성에 맞게 활용하는 것을 허용한다.
- **GPT-6 Astra를 포함한 상위 모델 사용을 허용한다.** 특히 DEEP 작업에서 적극 고려한다.
- 제품 옵션 **`작업속도 빠르게 (크레딧이 추가 사용됩니다.)`는 절대 사용하지 않는다.**
- 모델 선택·추론 강도와 추가 크레딧 가속 옵션은 별개다.
- 사용자의 별도 명시적 허가 없이는 이 금지 규칙을 해제하지 않는다.

## 모델 전환 검증 규칙

### 무엇이 전환 증거인가

다음 중 하나처럼 **실제 실행 환경이 제공한 정보**가 있어야 모델 전환을 검증된 것으로 본다.

- 제품의 모델 선택기/상태 UI가 현재 세션의 모델을 표시함
- 런타임 또는 에이전트 상태 정보가 모델 ID/이름을 반환함
- API 응답/로그가 실제 요청에 사용된 모델을 식별함
- 해당 환경의 공식 상태 명령이 현재 모델을 직접 확인해 줌

에이전트가 스스로 `나는 GPT-6 Astra다`라고 문장으로 출력하는 것, 답변 스타일이 달라지는 것, 쉬운 문제의 속도나 품질이 달라지는 것은 **전환 증거가 아니다.**

### 검증 불가능한 경우

모델 메타데이터가 노출되지 않으면 다음 형식을 사용한다.

`[에이전트: Codex | 요청 모델: GPT-6 Astra | 실제 모델: 미노출/검증 불가 | FAST]`

이 경우 `GPT-6 Astra 사용 중`, `전환 완료`, `현재 모델: GPT-6 Astra`라고 쓰지 않는다.

### 모델 변경 후

실제 전환 기능을 사용했다면 가능한 환경에서는 즉시 전환 전후 모델을 다시 확인한다.

- 확인 성공: `[전환 검증: GPT-5.6 Sol → GPT-6 Astra]`
- 확인 실패: `[전환 요청: GPT-6 Astra | 실제 적용 모델: 미노출/검증 불가]`

**라벨 변경을 모델 전환으로 취급하지 않는다.**

## 에이전트/모델/단계 선언 형식

실제 모델이 확인되는 경우:
- `[에이전트: ChatGPT | 실제 모델: GPT-5.6 Sol | FAST] 단순 수정이라 빠르게 처리합니다.`
- `[에이전트: ChatGPT | 실제 모델: GPT-6 Astra | DEEP] 핵심 구조를 검토합니다.`

현재 환경에서 모델을 바꿀 수 없지만 더 적합한 모델이 있는 경우:
- `[에이전트: ChatGPT | 실제 모델: GPT-5.6 Sol | 권장 모델: GPT-6 Astra | DEEP] 이 환경에서는 전환 권한이 없어 권장만 표시합니다.`

## Codex 모델 표기 규칙

**`Codex`라고만 표시하는 것을 금지한다.** 다만 실제 모델이 노출되지 않는데 임의의 모델명을 붙이는 것도 금지한다.

실제 모델이 확인될 때:
- `[에이전트: Codex | 실제 모델: GPT-6 Astra | DEEP]`
- `[에이전트: Codex | 실제 모델: GPT-5.6 Sol | NORMAL]`

모델이 미노출일 때:
- `[에이전트: Codex | 요청 모델: GPT-6 Astra | 실제 모델: 미노출/검증 불가 | DEEP]`
- `[에이전트: Codex | 실제 모델: 미노출/검증 불가 | NORMAL]`

금지:
- `[Codex] 작업을 시작합니다.`
- `[Codex | GPT-6 Astra]` ← 실제 모델 확인 근거가 없을 때
- `Codex이므로 GPT-6 Astra일 것이다.`

Codex의 기본 모델은 버전·환경·설정에 따라 달라질 수 있으므로 기본값을 현재 실행 모델로 간주하지 않는다.

## 작업 단계 전환

- `[FAST → NORMAL] 예상보다 영향 범위가 넓어 일반 개발 수준으로 승격합니다.`
- `[NORMAL → DEEP] 핵심 구조 변경이 필요해 깊은 분석으로 승격합니다.`
- `[DEEP → NORMAL] 핵심 원인이 확인되어 분석 강도를 낮춥니다.`
- `[NORMAL → FAST] 수정 지점이 명확해져 빠른 처리로 전환합니다.`

단계 전환은 모델 전환과 별개다. 같은 모델에서 FAST/NORMAL/DEEP만 바뀔 수 있고, 모델이 바뀌어도 작업 단계가 그대로일 수 있다.

**`FAST`는 작업 난이도 분류이며 `작업속도 빠르게 (크레딧이 추가 사용됩니다.)` 옵션을 뜻하지 않는다. 해당 유료 가속 옵션은 FAST에서도 사용하지 않는다.**

## 모델/추론 선택 원칙

### FAST
플랜에 포함된 경량·효율형 모델이 실제로 선택 가능한 환경이면 우선 고려한다. 모델 전환이 불가능한 현재 세션이라면 현재 모델을 그대로 사용하고 추론·탐색 범위만 최소화한다.

대상:
- 텍스트·수치·이름 변경
- CSS/UI 미세조정
- 명백한 단순 버그
- 기존 패턴 반복
- 로그·단순 조건문
- 수정 위치와 결과가 이미 명확한 작업

### NORMAL
일반 기능 구현, AI 행동 추가, UI 흐름, 저장/불러오기, 여러 함수가 연결된 버그, 작은 리팩터링, 게임 규칙 하나의 변경에 사용한다. 균형형 모델이 실제 선택 가능하면 사용한다.

### DEEP
핵심 게임 구조, 전투/AI/시뮬레이션 아키텍처, 대규모 데이터·저장 변경, 복합 원인 불명 버그, 성능 병목, 밸런스·경제·메타 설계, 대규모 리팩터링, 기술 스택 변경, 되돌리기 어려운 결정에 사용한다. GPT-6 Astra 등 최상위 모델이 실제 선택 가능하고 적용 여부를 확인할 수 있는 환경이면 적극 고려한다.

**기본 원칙: 모델 이름을 보여주는 것보다 실제 전환 여부를 정직하게 검증하는 것이 우선이다. 확인할 수 없으면 `미노출/검증 불가`라고 명시한다.**