# DRAFT ORDER — 에이전트 작업 시작 규칙

이 저장소에서 작업하는 모든 AI 에이전트는 코드·기획·UI·문서·테스트 등 관련 작업을 시작하기 전에 아래 순서를 따른다.

1. `docs/초원_AI_게임개발_협업_마스터프롬프트.md`를 먼저 읽고 현재 작업에 적용한다.
2. 작업을 **FAST / NORMAL / DEEP** 중 하나로 분류한다. 가능한 한 FAST에서 시작하고, 실제 복잡성이 확인될 때만 NORMAL → DEEP으로 승격한다.
3. 작업 시작 시 사용자에게 반드시 **현재 모델/모드 + 작업 단계**를 한 줄로 선언한다.
4. 실제 기반 모델이나 모드를 에이전트가 직접 바꿀 수 없는 환경이라면, 바꿨다고 거짓말하지 말고 **현재 모델**과 **권장 모델/모드**를 구분해서 표시한다.
5. 단계가 바뀌거나 더 높은/낮은 모델·모드가 적합해졌다면 즉시 다시 선언하고 이유를 한 줄로 설명한다.
6. 명확한 문제를 과도하게 분석하지 않는다. 실행해서 빠르게 확인 가능한 것은 작은 수정 → 실행/검증을 우선한다.
7. 최신 사용자 요청과 실제 저장소 상태가 문서보다 우선한다. 충돌 시 임의로 숨기지 말고 차이를 확인한다.
8. 작업 범위를 불필요하게 확장하지 않는다. 관련 파일 최소 범위부터 확인한다.
9. 구현 후에는 수행한 검증과 미확인 사항을 구분한다.

## 모델/모드 + 단계 선언 형식

작업 시작 예시:
- `[모델: 현재 모델 | 모드: Instant/빠른 처리 | FAST] 단순 수정이라 빠르게 처리합니다.`
- `[모델: 현재 모델 | 모드: 일반 추론 | NORMAL] 여러 함수가 연결되어 있어 일반 개발 수준으로 처리합니다.`
- `[모델: 현재 모델 | 모드: 높은 추론 권장 | DEEP] 핵심 구조 결정이 포함되어 깊게 검토합니다.`

모델을 직접 전환할 수 없는 환경:
- `[현재 모델: GPT-X | 권장: 빠른 모델/Instant | FAST] 이 작업은 더 가벼운 모델로도 충분합니다.`
- `[현재 모델: GPT-X | 권장: 높은 추론 모델/High | DEEP] 핵심 구조 문제라 높은 추론이 적합합니다.`

작업 중 전환 예시:
- `[FAST → NORMAL | 권장 모드: 일반 추론] 예상보다 영향 범위가 넓어 승격합니다.`
- `[NORMAL → DEEP | 권장 모드: 높은 추론] 핵심 구조 변경이 필요해 승격합니다.`
- `[DEEP → NORMAL | 권장 모드: 일반 추론] 핵심 원인이 확인되어 분석 강도를 낮춥니다.`
- `[NORMAL → FAST | 권장 모드: 빠른 처리] 수정 지점이 명확해져 빠른 처리로 전환합니다.`

**중요:** 에이전트가 실제 모델/모드를 바꿀 권한이 없으면 절대로 `전환 완료`라고 표현하지 않는다. 대신 `권장 전환`이라고 표시한다. 반대로 실제로 모델/모드 라우팅 기능이 있는 에이전트 환경이라면 작업 특성에 맞게 실제 전환하고 그 사실을 명시한다.

단계·모델 표시는 사용자에게 실제 작업 수준을 보여주기 위한 것이다. 형식만 붙이고 동일한 수준의 분석을 계속하지 마라.

## 모델/모드 선택 원칙

### FAST
가능하면 가장 빠른 모델/Instant 계열 또는 낮은 추론 강도를 사용한다.

대상:
- 텍스트·수치·이름 변경
- CSS/UI 미세조정
- 명백한 단순 버그
- 기존 패턴 반복
- 로그·단순 조건문
- 수정 위치와 결과가 이미 명확한 작업

최소 파일만 읽고 바로 수정·검증한다. 장문 설계나 전체 아키텍처 검토를 하지 않는다.

### NORMAL
일반 기능 구현에는 균형형 모델/모드 또는 중간 추론 강도를 사용한다.

대상:
- 일반 기능 구현
- AI 행동 추가
- UI 흐름
- 저장/불러오기
- 여러 함수가 연결된 버그
- 작은 리팩터링
- 게임 규칙 하나의 변경

관련 의존성만 확인하고 구현 → 검증의 짧은 사이클로 처리한다.

### DEEP
가장 강한 모델 또는 높은 추론 모드는 정말 필요한 경우에만 사용한다.

대상:
- 핵심 게임 구조
- 전투/AI/시뮬레이션 아키텍처
- 저장·데이터 구조 대규모 변경
- 복합 원인 불명 버그
- 성능 병목
- 밸런스·경제·메타 설계
- 대규모 리팩터링
- 기술 스택 변경
- 되돌리기 어려운 결정

**기본 원칙: 높은 지능과 긴 추론은 희소 자원이다. 문제 해결에 필요한 만큼만 사용한다.**
