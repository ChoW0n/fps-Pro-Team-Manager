# DRAFT ORDER — 에이전트 작업 시작 규칙

이 저장소에서 작업하는 모든 AI 에이전트는 코드·기획·UI·문서·테스트 등 관련 작업을 시작하기 전에 아래 순서를 따른다.

1. `docs/초원_AI_게임개발_협업_마스터프롬프트.md`를 먼저 읽고 현재 작업에 적용한다.
2. 작업을 **FAST / NORMAL / DEEP** 중 하나로 분류한다. 가능한 한 FAST에서 시작하고, 실제 복잡성이 확인될 때만 NORMAL → DEEP으로 승격한다.
3. 작업 시작 시 사용자에게 반드시 한 줄로 현재 단계를 선언한다. 예: `이번 작업은 FAST로 처리하겠습니다.`
4. 단계가 바뀌면 즉시 다시 선언하고 이유를 한 줄로 설명한다. 예: `NORMAL로 승격합니다 — 단순 UI 문제가 아니라 저장 상태와 함께 얽혀 있습니다.`
5. 명확한 문제를 과도하게 분석하지 않는다. 실행해서 빠르게 확인 가능한 것은 작은 수정 → 실행/검증을 우선한다.
6. 최신 사용자 요청과 실제 저장소 상태가 문서보다 우선한다. 충돌 시 임의로 숨기지 말고 차이를 확인한다.
7. 작업 범위를 불필요하게 확장하지 않는다. 관련 파일 최소 범위부터 확인한다.
8. 구현 후에는 수행한 검증과 미확인 사항을 구분한다.

## 단계 선언 형식

작업 시작:
- `[FAST] 이번 작업은 단순 수정이라 빠르게 처리하겠습니다.`
- `[NORMAL] 이번 작업은 여러 함수가 연결되어 있어 일반 개발 수준으로 처리하겠습니다.`
- `[DEEP] 이번 작업은 핵심 구조 결정이 포함되어 깊게 검토하겠습니다.`

작업 중 승격:
- `[FAST → NORMAL] 예상보다 영향 범위가 넓어 일반 개발 수준으로 승격합니다.`
- `[NORMAL → DEEP] 핵심 구조 변경이 필요해 깊은 검토 단계로 승격합니다.`

작업 중 강등:
- `[DEEP → NORMAL] 핵심 원인이 확인되어 더 이상 깊은 분석이 필요하지 않습니다.`
- `[NORMAL → FAST] 수정 지점이 명확해져 빠른 처리로 전환합니다.`

단계 표시는 사용자에게 실제 작업 수준을 보여주기 위한 것이다. 형식만 붙이고 실제로 동일한 수준의 분석을 계속하지 마라.

## 추론 예산

### FAST
텍스트·수치·이름 변경, CSS/UI 미세조정, 명백한 단순 버그, 기존 패턴 반복, 로그, 단순 조건문 등. 최소 파일만 읽고 바로 수정·검증한다. 장문 설계나 전체 아키텍처 검토를 하지 않는다.

### NORMAL
일반 기능 구현, AI 행동 추가, UI 흐름, 저장/불러오기, 여러 함수가 연결된 버그, 작은 리팩터링, 게임 규칙 하나의 변경. 관련 의존성만 확인하고 구현 → 검증의 짧은 사이클로 처리한다.

### DEEP
핵심 게임 구조, 전투/AI/시뮬레이션 아키텍처, 저장·데이터 구조 대규모 변경, 복합 원인 불명 버그, 성능 병목, 밸런스·경제·메타, 대규모 리팩터링, 기술 스택 변경, 되돌리기 어려운 결정에만 사용한다.

**기본 원칙: 높은 추론은 희소 자원이다. 문제 해결에 필요한 만큼만 사용한다.**
