# DRAFT ORDER — 에이전트 작업 시작 규칙

이 저장소에서 작업하는 모든 AI 에이전트는 코드·기획·UI·문서·테스트 등 관련 작업을 시작하기 전에 아래 순서를 따른다.

1. `docs/초원_AI_게임개발_협업_마스터프롬프트.md`를 먼저 읽고 현재 작업에 적용한다.
2. 작업을 **FAST / NORMAL / DEEP** 중 하나로 분류한다. 가능한 한 FAST에서 시작하고, 실제 복잡성이 확인될 때만 NORMAL → DEEP으로 승격한다.
3. **모델·에이전트·강도가 실제로 변경될 때만** 사용자에게 한 줄을 표시한다.
4. 표시 형식은 반드시 `에이전트/모델/강도`만 사용한다. 설명 문장을 붙이지 않는다.
5. 모델을 확인할 수 없는 환경에서는 추정하지 않고 `미노출`로 적는다.
6. 모델이나 단계가 바뀌지 않았다면 같은 표기를 반복하지 않는다.
7. 명확한 문제를 과도하게 분석하지 않는다. 실행해서 빠르게 확인 가능한 것은 작은 수정 → 실행/검증을 우선한다.
8. 최신 사용자 요청과 실제 저장소 상태가 문서보다 우선한다.
9. 작업 범위를 불필요하게 확장하지 않는다.
10. 구현 후에는 수행한 검증과 미확인 사항을 구분한다.

## 사용자 플랜 및 비용 정책

- 사용자는 **Pro*5 플랜에서 사용 가능한 모든 모델**을 작업 특성에 맞게 활용하는 것을 허용한다.
- **GPT-6 Astra를 포함한 상위 모델 사용을 허용한다.** 특히 DEEP 작업에서 적극 고려한다.
- 제품 옵션 **`작업속도 빠르게 (크레딧이 추가 사용됩니다.)`는 절대 사용하지 않는다.**
- 모델 선택·추론 강도와 추가 크레딧 가속 옵션은 별개다.
- 사용자의 별도 명시적 허가 없이는 이 금지 규칙을 해제하지 않는다.

## 표시 규칙

형식:

`에이전트/모델/강도`

예시:

`Codex/GPT-6 Astra/DEEP`

`ChatGPT/GPT-5.6 Sol/NORMAL`

`Codex/미노출/FAST`

다음은 금지한다.

- 대괄호, 설명 문장, 전환 이유를 같은 줄에 덧붙이는 것
- `현재 모델:`, `권장 모델:`, `에이전트:` 같은 라벨
- 모델·에이전트·강도가 바뀌지 않았는데 매 응답마다 같은 표기를 반복하는 것
- 실제 확인되지 않은 모델명을 현재 사용 모델처럼 쓰는 것

**표시는 처음 작업을 시작할 때 한 번, 또는 이후 실제 모델/에이전트/강도가 변경될 때 한 번만 출력한다.**

## 모델 전환 검증 규칙

모델 전환이 확인되었다고 판단하려면 제품의 모델 선택기/상태 UI, 런타임 상태, API 응답/로그, 공식 상태 명령 등 실제 실행 환경의 근거가 있어야 한다.

에이전트가 스스로 특정 모델이라고 말하는 것, 답변 스타일·속도·품질 차이는 모델 전환 증거가 아니다.

모델 정보가 노출되지 않으면 표시의 모델 자리에 `미노출`을 사용한다.

예:

`Codex/미노출/NORMAL`

라벨 변경을 모델 전환으로 취급하지 않는다.

## 작업 단계

### FAST
텍스트·수치·이름 변경, CSS/UI 미세조정, 명백한 단순 버그, 기존 패턴 반복, 로그·단순 조건문, 수정 위치와 결과가 이미 명확한 작업.

가능하면 최소 파일만 읽고 바로 수정·검증한다. **`작업속도 빠르게 (크레딧이 추가 사용됩니다.)` 옵션과는 무관하며 해당 옵션은 사용하지 않는다.**

### NORMAL
일반 기능 구현, AI 행동 추가, UI 흐름, 저장/불러오기, 여러 함수가 연결된 버그, 작은 리팩터링, 게임 규칙 하나의 변경.

관련 의존성만 확인하고 구현 → 검증의 짧은 사이클로 처리한다.

### DEEP
핵심 게임 구조, 전투/AI/시뮬레이션 아키텍처, 대규모 데이터·저장 변경, 복합 원인 불명 버그, 성능 병목, 밸런스·경제·메타 설계, 대규모 리팩터링, 기술 스택 변경, 되돌리기 어려운 결정.

GPT-6 Astra 등 최상위 모델이 실제 선택 가능하고 적용 여부를 확인할 수 있는 환경이면 적극 고려한다.

**기본 원칙: 모델 이름을 보여주는 것보다 실제 전환 여부를 정직하게 검증하는 것이 우선이다. 확인할 수 없으면 `미노출`이라고 표시한다.**