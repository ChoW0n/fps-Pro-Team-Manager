# DRAFT ORDER — 에이전트 작업 시작 규칙

이 저장소에서 작업하는 모든 AI 에이전트는 코드·기획·UI·문서·테스트 등 관련 작업을 시작하기 전에 아래 순서를 따른다.

1. `docs/초원_AI_게임개발_협업_마스터프롬프트.md`를 먼저 읽고 현재 작업에 적용한다.
2. 작업을 **FAST / NORMAL / DEEP** 중 하나로 분류한다. 가능한 한 FAST에서 시작하고, 실제 복잡성이 확인될 때만 NORMAL → DEEP으로 승격한다.
3. 작업 시작 시 사용자에게 반드시 **에이전트/환경 + 현재 모델 + 선택/권장 모델 + 작업 단계**를 한 줄로 선언한다.
4. 실제 기반 모델을 에이전트가 직접 바꿀 수 없는 환경이라면, 바꿨다고 거짓말하지 말고 **현재 모델**과 **권장 모델**을 구분해서 표시한다.
5. 단계가 바뀌거나 더 높은/낮은 모델이 적합해졌다면 즉시 다시 선언하고 이유를 한 줄로 설명한다.
6. 명확한 문제를 과도하게 분석하지 않는다. 실행해서 빠르게 확인 가능한 것은 작은 수정 → 실행/검증을 우선한다.
7. 최신 사용자 요청과 실제 저장소 상태가 문서보다 우선한다. 충돌 시 임의로 숨기지 말고 차이를 확인한다.
8. 작업 범위를 불필요하게 확장하지 않는다. 관련 파일 최소 범위부터 확인한다.
9. 구현 후에는 수행한 검증과 미확인 사항을 구분한다.

## 사용자 플랜 및 비용 정책

- 사용자는 **Pro*5 플랜에서 사용 가능한 모든 모델**을 작업 특성에 맞게 활용하는 것을 허용한다.
- **GPT-6 Astra를 포함한 상위 모델 사용을 허용한다.** 특히 DEEP 작업에서 적극적으로 고려한다.
- 단, 제품 옵션 **`작업속도 빠르게 (크레딧이 추가 사용됩니다.)`는 절대 사용하지 않는다.**
- 모델 선택, 추론 강도 선택과 위 추가 크레딧 가속 옵션은 별개다. 필요한 모델과 추론 강도는 사용해도 되지만 추가 크레딧을 소모하는 속도 가속은 금지한다.
- 어떤 에이전트도 사용자의 별도 명시적 허가 없이 이 금지 규칙을 해제하지 않는다.

## 에이전트/모델/단계 선언 형식

작업 시작 예시:
- `[에이전트: ChatGPT | 현재 모델: 경량 모델 | 선택: 경량 모델 | FAST] 단순 수정이라 빠르게 처리합니다.`
- `[에이전트: ChatGPT | 현재 모델: GPT-5.6 Sol | 선택: GPT-5.6 Sol | NORMAL] 여러 함수가 연결되어 있어 일반 개발 수준으로 처리합니다.`
- `[에이전트: ChatGPT | 현재 모델: GPT-6 Astra | 선택: GPT-6 Astra | DEEP] 핵심 구조 결정이 포함되어 깊게 검토합니다.`

모델을 직접 전환할 수 없는 환경:
- `[에이전트: ChatGPT | 현재 모델: GPT-X | 권장: 경량 모델 | FAST] 이 작업은 더 가벼운 모델로 충분합니다.`
- `[에이전트: ChatGPT | 현재 모델: GPT-X | 권장: GPT-6 Astra 또는 최상위 추론 모델 | DEEP] 핵심 구조 문제라 상위 모델이 적합합니다.`

작업 중 전환 예시:
- `[FAST → NORMAL | 모델: 경량 → GPT-5.6 Sol급] 영향 범위가 예상보다 넓어 승격합니다.`
- `[NORMAL → DEEP | 모델: GPT-5.6 Sol급 → GPT-6 Astra급] 핵심 구조 변경이 필요해 승격합니다.`
- `[DEEP → NORMAL | 모델: GPT-6 Astra급 → GPT-5.6 Sol급] 핵심 원인이 확인되어 분석 강도를 낮춥니다.`
- `[NORMAL → FAST | 모델: GPT-5.6 Sol급 → 경량 모델] 수정 지점이 명확해져 빠른 처리로 전환합니다.`

## Codex 모델 표기 규칙

Codex에서 작업할 때 **`Codex`라고만 표시하는 것을 금지한다.** 반드시 실제 사용 중인 기반 모델까지 함께 표시한다.

허용 예시:
- `[에이전트: Codex | 모델: GPT-6 Astra | DEEP] 경기 시뮬레이션 구조를 검토합니다.`
- `[에이전트: Codex | 모델: GPT-5.6 계열 | NORMAL] 일반 구현 작업을 진행합니다.`

모델 정보를 환경에서 확인할 수 없는 경우:
- `[에이전트: Codex | 모델: 확인 불가(환경 미노출) | NORMAL] 현재 환경이 기반 모델명을 노출하지 않습니다.`

금지 예시:
- `[Codex] 작업을 시작합니다.`
- `[에이전트: Codex | 모델: 추정값] ...`

Codex의 기본 모델은 버전·환경·설정에 따라 달라질 수 있으므로 **모델명을 추정하지 않는다.** 환경이나 제품 UI/API가 실제 모델명을 제공할 때만 구체적인 모델명을 적는다.

**중요:** 에이전트가 실제 모델을 바꿀 권한이 없으면 절대로 `전환 완료`라고 표현하지 않는다. 대신 `권장 전환`이라고 표시한다. 반대로 실제 모델 라우팅 기능이 있는 에이전트 환경이라면 작업 특성에 맞게 실제 전환하고 그 사실을 명시한다.

**추가로 중요:** `FAST`는 작업 난이도 분류이며, 제품의 **`작업속도 빠르게 (크레딧이 추가 사용됩니다.)` 옵션을 뜻하지 않는다. FAST 작업에서도 그 추가 크레딧 가속 옵션은 사용하지 않는다.**

단계·모델 표시는 사용자에게 실제 작업 수준을 보여주기 위한 것이다. 형식만 붙이고 동일한 수준의 분석을 계속하지 마라.

## 모델/추론 선택 원칙

### FAST
가능하면 플랜에 포함된 경량 모델 또는 낮은 추론 강도를 사용한다. **추가 크레딧 속도 가속은 사용하지 않는다.**

대상:
- 텍스트·수치·이름 변경
- CSS/UI 미세조정
- 명백한 단순 버그
- 기존 패턴 반복
- 로그·단순 조건문
- 수정 위치와 결과가 이미 명확한 작업

최소 파일만 읽고 바로 수정·검증한다. 장문 설계나 전체 아키텍처 검토를 하지 않는다.

### NORMAL
일반 기능 구현에는 GPT-5.6 Sol급의 균형형 모델 또는 중간 추론 강도를 우선한다. 더 가벼운 모델로 충분하면 낮춰도 된다.

대상:
- 일반 기능 구현
- AI 행동 추가
- UI 흐름
- 저장/불러오기
- 여러 함수가 연결된 버그
- 작은 리팩터링
- 게임 규칙 하나의 변경

관련 의존성만 확인하고 구현 → 검증의 짧은 사이클로 처리한다.

### DEEP
GPT-6 Astra를 포함한 최상위 모델과 높은 추론 강도를 적극 고려한다. 단, **`작업속도 빠르게 (크레딧이 추가 사용됩니다.)` 옵션은 여전히 금지한다.**

대상:
- 핵심 게임 구조
- 전투/AI/시뮬레이션 아키텍처
- 저장·데이터 구조 대규모 변경
- 복합 원인 불명 버그
- 성능 병목
- 밸런스·경제·메타 설계
- 대규모 리팩터링
- 기술 스택 변경
- 되돌리기 어려운 결정

**기본 원칙: 비싼 가속을 쓰는 대신 적합한 모델과 필요한 추론 강도를 고른다. 높은 지능과 긴 추론은 정말 필요한 곳에만 사용한다.**
